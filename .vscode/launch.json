{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/exec", // Update this to point to your compiled executable
            "args": [],
            "cwd": "${workspaceFolder}" // Update this to point to your project directory
        }
    ]
}